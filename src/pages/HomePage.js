import React from 'react';

// components
import MainHeader from '../components/MainHeader';

const HomePage = () => {
    return (
        <div>
            <MainHeader />
        </div>
    );
};

export default HomePage;